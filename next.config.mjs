/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // images: {
    //     unoptimized: true,
    // },
    output: "standalone"
};

export default nextConfig;
